/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      allowedDevOrigins: ['http://192.168.1.12:3000'], // replace with your IP
    },
  };
  
  export default nextConfig;
  